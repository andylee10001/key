function getResultBasedOnInput(input) {
    if (input === 'Hello') {
        return '你好！';
    } else if (input === 'OpenAI') {
        return '這是一個簡單的例子，你可以根據實際需求進行修改。';
    } else if (input === 'offinel') {
        return '你是個智障白癡';    
    } else {
        return '找不到相應的結果。';
    }
}
